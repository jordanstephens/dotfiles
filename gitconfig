[user]
  name = Jordan Stephens
  email = iam@jordanstephens.net
[core]
  editor = vim
  excludesfile = ~/.gitignore
[help]
	autocorrect = 1
[color]
  diff = auto
  status = auto
  branch = auto
[color "status"]
  added = cyan
  untracked = red
  changed = green
[alias]
  st = status -sb
  co = checkout
  ci = commit
  df = diff
  ds = diff --stat
  search = log -S
  # delete local tags, replace with remote tags
  synctags = !git tag -l | xargs git tag -d && git fetch --tags

